# Optional dependencies for the offline ONNX export scripts only.
# The runtime service (app/...) does NOT need any of these.
#
# Install on top of requirements.txt when you need to (re)export weights:
#   pip install -r requirements-export.txt
#
# Scripts that need this:
#   scripts/export_hrnet_onnx.py    (PnLCalib HRNet -> ONNX)
#   scripts/export_dinov3_onnx.py   (DINOv3 ViT-S+/16 -> ONNX)

torch==2.5.1
torchvision==0.20.1
transformers==4.57.6   # for DINOv3 weights download + loader
onnx==1.21.0           # required by torch.onnx.export
