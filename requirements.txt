# --- FastAPI + server ---
fastapi==0.115.6
uvicorn[standard]==0.32.1
python-multipart==0.0.17

# --- ML / CV runtime (GPU) ---
# onnxruntime-gpu 1.20+ for CUDA 12.x; on WSL2 see app/utils/cuda_env.py
onnxruntime-gpu==1.20.1
opencv-python-headless==4.10.0.84
numpy==1.26.4
scikit-learn==1.6.0           # GMM (team clustering), KMeans (legacy)
scipy==1.14.1                 # Hungarian, maximum_filter, linregress, spatial.cdist
Pillow==11.0.0
PyYAML==6.0.2                 # PnLCalib hrnet configs
shapely==2.1.2                # PnLCalib utils_optimize

# --- Tracking ---
# BoT-SORT is vendored (torch-free) under app/vendor/boxmot/ (AGPL-3.0,
# see app/vendor/boxmot/__init__.py). The pip `boxmot` package is no longer
# required; only its runtime dependencies remain:
lap==0.9.4                    # linear assignment used by vendored BoT-SORT
loguru==0.7.3                 # logger used by vendored boxmot

# --- Database ---
sqlalchemy==2.0.36
aiosqlite==0.20.0
alembic==1.14.0
greenlet==3.1.1

# --- Authentication ---
python-jose[cryptography]==3.3.0
passlib[bcrypt]==1.7.4
bcrypt==4.2.1

# --- Utils ---
pydantic==2.10.3
pydantic-settings==2.6.1
python-dotenv==1.0.1
gdown==5.2.0

# NOTE: torch / torchvision / transformers / onnx are NOT required at runtime.
# They are only needed for the offline ONNX export scripts under scripts/.
# Install them from `requirements-export.txt` if you need to (re)export models.
